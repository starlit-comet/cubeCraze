<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CubeCraze Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #14576f;
        }
        .login-container {
            max-width: 900px;
            margin: 50px auto;
            background: #fff;
            border-radius: 10px;
            overflow: hidden;
            display: flex;
            box-shadow: 0px 0px 20px rgba(150, 193, 63, 0.2);
        }
        .login-form {
            flex: 1;
            padding: 40px;
        }
        .login-image {
            flex: 1;
            background: url('/img/home-logo-1-1.png') no-repeat center;
            background-size: cover;
        }
        .form-control {
            border-radius: 20px;
        }
        .btn-custom {
            border-radius: 20px;
            width: 100%;
        }
        .social-login button {
            width: 100%;
            border-radius: 20px;
            margin-top: 10px;
        }
        .social-icon {
            width: 20px;
            height: 20px;
            margin-right: 8px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-form">
            <h3 class="text-center fw-bold">LOGIN</h3>
            <p class="text-center text-muted">Login With Your Credentials to continue Shopping</p>
            <form action='signin' method="post">
                <div class="mb-3">
                    <input type="email" name="email" class="form-control" value="" placeholder="Email">
                </div>
                <div class="mb-3">
                    <input type="" name="password" class="form-control" placeholder="Password">
                </div>
                
                <div class="d-flex gap-2 mt-3">
                    <button type="submit" class="btn btn-primary btn-custom">Login Now</button>
                    <button type="button" class="btn btn-outline-primary btn-custom"  onclick="redirectToSignup()">Sign up</button>
                </div>
            </form>
            <hr>
            <p class="text-center">Login with Others</p>
            
        </div>
        <div class="login-image"></div>

    </div>

    <script>
        function redirectToSignup() {
            // Change the URL to your actual signup success or home page
            window.location.href = "signup";
        }
    </script>
    
</body>
</html>
 